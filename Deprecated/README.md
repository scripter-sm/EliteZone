`old_assets` contains the assets for the gui inside `old_sources`.
`old_compiler` and `old_workflows` contain the compiler for the gui inside `old_sources`.
`old_libraries` just contains my old fork of the linoria library and compiled fork of vape v4 gui.
